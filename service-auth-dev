eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8082
spring:
  application:
    name: service-auth
  datasource:
      password: orlando123
      url: jdbc:postgresql:market_ms
      username: postgres
  jpa:
      database-platform: org.hibernate.dialect.PostgreSQL9Dialect
app:
    jwt:
        expiration: 900000
        header: Authorization
        header.prefix: 'Bearer '
        secret: mySecret
